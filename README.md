# Mini KOF（迷你拳皇）

一个可以直接在浏览器里打开的双人格斗小游戏（本地键盘对战）。

## 玩法
- P1：`A` / `D` 移动，`W` 跳跃，`F` 攻击
- P2：`←` / `→` 移动，`↑` 跳跃，`L` 攻击
- `R`：重开一局

## 启动
直接双击 `index.html` 打开即可，或使用任意静态服务器。
例如：

```bash
python3 -m http.server 8000
```

然后访问：`http://localhost:8000`
